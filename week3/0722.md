# 1. 리뷰

## 오늘 한 작업

### 수업

- JVM
    - Class Loader
    - Runtime Data Area
    - Execution Engine
- Garbage Collection
    - Young Generation / Old Generation
    - Minor GC / Major GC

<br>

### 웹 서버 7단계 미션

- DB 관련 동작 수행 시 트랜잭션 고려
- HttpResponseSender는 응답 전송만 하도록
- UserDatabase.addUser() 중복 아이디 검사 로직 따로 빼기
- DB 초기 세팅 로직 추출
- 로그인이 필요한 페이지에 대한 전처리 수행

<br>

### 종합프로젝트

- 팀 회의
    - 프로세스 플로우
    - 디자인 테마

<br>

## 작업 과정에서 배운 것

### 1. 생각은 다양할수록 좋다

오늘도 그룹 세션을 하면서, 그룹 멤버들과 미션 수행 내용을 서로 공유했다.

그 과정에서 예외 처리 방식, 다양한 HTTP body 타입에 대한 대응 방법, DB 트랜잭션 처리 등 내가 미처 신경 쓰지 못했던 세세한 부분들을 꼼꼼하게 고려한 코드들을 볼 수 있었다.

벌써 3주차에 접어들며 여러 사람과 코드를 공유해왔지만, 이번 주에는 새로운 멤버들과 함께하며 지금까지 한 번도 받지 못했던 피드백을 새롭게 접할 수 있었다. 그 속에서 내가 놓친 부분을 발견하기도 했고, 더 나은 구현 방식을 배우기도 했다.

사람들의 생각은 다양할수록 좋다는 것을 다시 한 번 깨달았다. 배워야 할 것들이 무궁무진하다는 것도 다시 한 번 깨달았다. 앞으로도 다양한 코드와 설계들을 접하면서, 나의 외연을 계속 넓혀나가고 싶다.

<br>

### 2. 시각적인 정리의 힘

수업 시간에 JVM의 구조와 동작에 대해 다시 다뤘다.

저번 주에는 개념을 전반적으로 이해하기는 했지만, 내부 동작 흐름에 대해서는 다소 막연하게 알고 넘어간 감이 있었다.

이번 주 수업에서는 Dangle 님이 칠판에 그림을 그려가며 설명해주신 덕분에, 추상적인 개념들이 머릿속에서 구체적인 이미지로 정리되었다. 전보다 흐름이 한층 명확하게 이해되었고, 복습할 때도 더 잘 떠오를 것 같았다.

텍스트보다는 시각적인 요소가 이해하기도 쉽고, 기억에 잘 남는다는 것을 다시금 느꼈다. 앞으로도 공부를 하거나 개발 관련 구상을 할 때, 시각적으로 구조를 정리해보는 방식을 적극적으로 활용해봐야겠다.

<br>

## 어려웠던 점

### 1. 책임 분리와 유효성 검사

`UserDatabase.addUser()` 메서드는 새로운 회원을 데이터베이스에 삽입하는 기능을 담당한다. 그런데 이 기능을 호출하기 전에, 입력값에 대한 검증이 반드시 선행되어야 했다.

기능 수행과 그에 앞선 검증은 실제로는 거의 항상 붙어다니는 로직이다. 그래서 처음엔 검증까지 하나의 메서드에서 처리해서, 실수 없이 안전하게 동작하도록 보장하는 게 나을까 하는 생각도 들었다.

하지만 ‘역할 분리’라는 관점에서 보면, `addUser()`는 오직 삽입에 대한 책임만을 가져야 한다. 검증에 대한 책임까지 함께 가지고 있는 것이 약간 어색하게 느껴졌다. 결국 검증은 외부에서 처리하도록 하여 책임을 분리해주었다.

문제는, 이렇게 하면 검증 없이 `addUser()`를 호출할 수 있는 여지가 생긴다는 점이다. 그렇다고 `addUser()` 내부에 검증 로직을 한 번 더 넣자니, 로직이 중복되거나, 책임의 경계가 모호해진다는 것이 또 문제였다. 이러한 딜레마를 어떤 식으로 해소할 수 있을지 조금 더 생각해봐야겠다.

<br>

# 2. 회고

## Keep

### **1. 서로의 생각에 대해 완벽하게 이해하고 넘어가기**

- 종합 프로젝트를 하면서, 여러 방식 중 어떤 것이 더 나은지에 대한 논의를 진행했다.
- 이 과정에서 비슷한 의견인데 다르다고 오해하거나, 반대로 전혀 다른 의견을 비슷하다고 착각하는 상황이 몇 차례 있었다.
- 서로가 어떤 전제를 바탕으로 이야기하고 있는지를 먼저 명확히 하지 않으면, 생산적인 논의가 어려워진다.
- 상대방의 생각을 들었을 때, 내가 제대로 이해했는지 다시 확인하는 습관을 들이고 있다.
- 불필요한 시행착오를 줄이고, 정확한 의사소통을 할 수 있었다.

### 2. **피드백을 곱씹어보고 흡수하기**

- 코드 리뷰나 세션에서 받은 피드백들을 그때그때 기록해두는 습관을 들이고 있다.
- 귀로만 듣고 넘어가면, 금방 까먹어버리고 발전으로 이어지지 않는 경우가 많다.
- 지금은 기록해둔 내용을 다시 읽어보고, 현재 방식과 비교하며 어떻게 개선할 수 있을지 고민해보고 있다.
- 피드백을 소중히 여기면서 고칠 점을 계속 찾아나가는 자세를 유지하고 싶다.

<br>

## Problem

### **1. 커밋 메세지의 형식이 일관적이지 못하다**

- 커밋 메시지를 쓸 때, 가능한 한 작업 흐름을 잘 설명하려고 노력하고 있다.
- 각각의 메시지 자체는 점점 나아지고 있지만, 메시지들의 전체 목록을 한눈에 봤을 때는 형식이 들쑥날쑥해서 정돈되지 않아 보인다는 문제가 있었다.
- 협업을 생각한다면, 형식 면에서도 더 신경을 써야 할 것 같다.

### **2. 모르는 건 고민해도 안 보인다**

- "아는 만큼 보인다"는 말도 있듯이, 코드에 대해 아무리 깊게 고민한다고 해도 시야가 좁으면 보이지 않는 영역들이 분명히 있다.
- 경험이 쌓이면 자연스럽게 보이는 것들이 많아지겠지만, 그 과정은 시간도 오래 걸리고, 기회 자체가 자주 오는 것도 아니다.
- 시야를 넓히기 위한 별도의 노력이 필요하다는 것을 느꼈다.

<br>

## Try

### **1. 자기만의 커밋 메세지 형식을 정해두자**

- 커밋 메세지 형식에 정답이라는 것은 없다지만, 적어도 자기 커밋 메세지들 사이에는 일관된 형식과 규칙이 있어야 한다고 생각한다.
- 읽는 사람이 중요한 정보를 쉽고 빠르게 파악할 수 있도록, 하나의 형식으로 통일되게 커밋 메세지를 작성해보자.

### **2. 간접 경험도 쌓아보자**

- 요즘은 블로그, 유튜브 등 다양한 채널을 통해 다른 개발자들의 경험과 고민을 접할 수 있다.
- 매일 10분이라도 이런 콘텐츠를 읽으면서 내가 겪어보지 못한 문제와 그에 대한 해결 과정을 미리 알아두자. 비슷한 상황을 마주했을 때, 선택의 폭과 대응할 수 있는 능력 모두 달라질 것이다.

<br>

# 3. 학습내용

## JVM

### JVM (Java Virtual Machine)

- 자바 바이트코드를 실행하는 가상 머신
- 메모리 관리, gc, 스레드 관리
- 플랫폼 독립성 보장
- 메모리 관리, 성능 확인을 위해서는 JVM 레벨까지 들어가야 함
- **Write Once Run Everywhere**

## JVM의 구조

### 1. Class Loader

- .class 파일(바이트코드)을 JVM으로 불러오는 역할
- Runtime Data Area에 저장시킴
- 세 단계로 구성
    - **Loading**: 클래스 파일을 찾고, 바이트코드를 읽어서 JVM 메모리에 적재
    - **Linking**: 클래스의 기본 구조를 검증하고, 심볼을 실제 참조로 연결
    - **Initialization**: static 초기화 블록과 static 필드를 실제 값으로 초기화
- 동적 로딩
    - 필요할 때 한 번만 로딩
    - 유연성 강화
    - 성능 강화

### 2. Runtime Data Area

- JVM이 실행 시에 사용하는 메모리 공간
- **Method Area**
    - 클래스 로더가 읽어들인 클래스의 메타정보 저장 → **잘 안 바뀌는 정보**
    - 클래스 이름, 메서드 이름, static 변수, 상수 풀 등
- **Heap**
    - new로 생성한 객체가 저장되는 공간
    - **GC(Garbage Collector)의 대상**이 되는 영역
    - **모든 스레드가 공유**함
- **Stack**
    - 각 **스레드마다 별도**로 존재 → 동시성 문제 해결
    - 메서드 호출 시 스택 프레임 생성
    - 지역변수, 매개변수, 반환주소 등 저장
- **PC Register**
    - 현재 실행 중인 명령어의 위치 저장
    - JVM은 스레드마다 하나의 PC 레지스터를 가짐
- **Native Method Stack**
    - C, C++ 등 네이티브 메서드를 호출할 때 사용하는 스택

### **3. Execution Engine**

- 로드된 바이트코드를 실제로 실행하는 부분
- 실행 중 필요한 정보를 Runtime Data Area에서 가져옴
- **Interpreter**
    - 바이트코드를 한 줄씩 **해석하며 실행** → 기계어로 바꾸는 것과는 다름
        - 시작은 빠르지만 속도가 느림
- **JIT(Just-In-Time) Compiler**
    - 자주 실행되는 코드(핫스팟)를 **기계어로 컴파일해서 캐시**
        - Interpreter를 거치지 않고 바로 실행 가능
        - 실행 속도 대폭 향상
- **Garbage Collection**
    - 실행 도중 더 이상 참조되지 않는 객체를 자동으로 탐지하고 메모리 회수
    - 개발자가 명시적으로 메모리를 해제하지 않아도 됨

### **4. Native Method Interface**

- 자바 코드에서 C, C++ 등의 네이티브 메서드 라이브러리를 사용할 수 있게 해주는 인터페이스

<br>

## 가비지 컬렉션(Garbage Collection)이 필요한 이유

### 가비지

- 더 이상 사용되지 않는 객체(인스턴스)
- ex) 지역 변수로 만들었지만 함수 끝나고 참조가 끊긴 객체

### 가비지 컬렉션(Garbage Collection)

- 자바는 개발자가 직접 메모리 해제를 하지 않음
- JVM이 쓸모없는 객체를 찾아서 자동으로 메모리에서 제거

### 가비지 컬렉션이 필요한 이유

1. **메모리 누수 방지**

- 메모리를 해제하지 않으면, 더 이상 쓰이지 않는 객체가 계속 힙 메모리에 남아 있음
    - 이런 객체들이 많아지면 메모리 누수(memory leak) 발생
    - OutOfMemoryError 발생 가능

2. **개발자의 실수 줄이기**

- C나 C++ 같은 언어에서는 `malloc/free`, `new/delete`로 직접 메모리 관리를 해야 함
    - 언제 객체를 해제해야 할지 판단하기가 어렵고, 실수도 많음.
- 자바는 자동으로 메모리를 회수해주기 때문에 개발자가 로직에만 집중할 수 있음

3. **메모리 자원의 효율적인 사용**

- 사용되지 않는 객체를 제거함으로써 힙 메모리를 재사용할 수 있게 해줌.
    - 동일한 메모리 공간을 여러 번 재활용할 수 있어 효율적

4. **프로그램의 안정성과 유지보수 향상**

- 수동 메모리 관리는 실수가 많고 디버깅도 어려움
    - 자바는 자동 GC 덕분에 안정적인 메모리 관리가 가능
    - 유지보수가 쉬워짐

### 자바 가비지 컬렉션의 작동 원리

- JVM은 힙 영역을 크게 두 가지로 나누어 관리
    - 대부분의 객체는 금방 사라진다는 통계적 사실을 바탕으로 설계된 구조
    - 일시적인 객체는 빠르게 정리하고, 오래 사는 객체는 효율적으로 관리하는 전략

**1. Young Generation (영 제너레이션)**

- 새로 생성된 객체들이 저장되는 공간
- 대부분의 객체는 여기에서 생성되고, 빠르게 사라짐
    - Eden(에덴): 대부분의 객체가 여기에 생성됨.
    - Survivor S0 / S1: 에덴에서 살아남은 객체들이 여기에 복사됨. (두 개를 번갈아 사용)

**2. Old Generation (올드 제너레이션)**

- Young에서 오래 살아남은 객체들이 이쪽으로 옮겨짐 (Promotion)
    - ex) 싱글톤 객체, 캐시, 세션 등 장기 사용 객체
- GC가 자주 일어나지 않지만, 발생하면 시간이 오래 걸려서 Stop-the-World 현상이 큼.

### 가비지 컬렉션 종류

**Minor GC**

- Young Generation에서 발생
- Eden 영역이 가득 차면 발생
- 살아남은 객체들은 Survivor 영역으로 이동하거나 Old로 승격됨
- 빠르고 자주 발생

**Major(Full) GC**

- Old Generation에서 발생
- 메모리가 부족하거나 시스템이 강제로 요청할 때 수행
- 속도가 느리고 CPU 사용량이 높아 전체 성능에 영향 줄 수 있음

<br>

## Thread-Safe = 스택에 저장?

1. volatile
2. atomic
3. synchronized
4. thread local
- 동기화 메커니즘이 있어서 안전할 뿐, 힙에 저장됨
    - 장점: 힙에 저장되므로 자원을 재사용 가능, GC 가능
    - 단점: Lock을 사용해야 함 → 속도 느려짐

<br>

# 4. 추가로 생긴 궁금점

- BiFunction
- enum 응용 방식
    - 이넘 내에서 메서드 사용
    - 람다 표현식

<br>

# 5. 추후 작업할 내용

- 예외 처리
    - 경로 뒤에 슬래시를 붙이는 경우
